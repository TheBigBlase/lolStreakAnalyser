import json
#prob gonna use jupyter notebook from here TODO do that later

class Analyser:
    """
    Class used to analyse previously extracted data
    """

    def __init__(self, summoner_name:str):
        #self.summary_file_path = f"out/{sanitizeSummonerName(summoner_name)}_summuary.json"
        #self.summuary = json.loads(self.summary_file_path)
        pass

    def main(self):
        """testing stuff"""
        pass
        #print(json.dumps(self.summuary, indent=2))
